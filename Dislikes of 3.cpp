#include <iostream>
#include <string>

int main() {
    int number = 9; // Example number

    // Convert the number to a string
    std::string decimalRepresentation = std::to_string(number);

    // Print the decimal representation
    std::cout << "Decimal representation: " << decimalRepresentation << std::endl;

    return 0;
}
